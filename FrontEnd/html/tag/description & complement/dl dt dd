h2. HTML <dl> Tag

The <code><dl></code> tag defines a description list.
The <code><dl></code> tag is used in conjunction with <code><dt></code> (defines terms/names) and <code><dd></code> (describes each term/name).

<dl>
  <dt>Coffee</dt>
  <dd>Black hot drink</dd>
  <dt>Milk</dt>
  <dd>White cold drink</dd>
</dl>